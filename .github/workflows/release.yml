# ═══════════════════════════════════════════════════════════════════════════
# release.yml — zapit release pipeline.
#
# Fires on every `v*` tag push (and on demand via workflow_dispatch):
#   release — bundles the source + docs snapshot and cuts the GitHub Release
#   with the bundle + checksums and generated release notes.
#
# Image publishing happens in docker.yml (same v* tags → GHCR); this workflow
# owns the GitHub Release with the source bundle + checksums.
# ═══════════════════════════════════════════════════════════════════════════

name: zapit release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version tag to build (default: use the pushed tag)"
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: zapit-release
  cancel-in-progress: false

jobs:
  release:
    name: Cut the zapit release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve release version
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
            if [[ ! "$VERSION" =~ ^v ]]; then
              VERSION="v$VERSION"
            fi
          else
            VERSION="${GITHUB_REF_NAME}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "release: $VERSION"

      - name: Build the source bundle
        run: |
          set -euo pipefail
          VERSION="${{ steps.tag.outputs.version }}"
          ARCHIVE="zapit-${VERSION#v}.tar.gz"
          mkdir -p dist
          tar \
            --exclude='./.git' \
            --exclude='./.github' \
            --exclude='./.gitignore' \
            -czf "dist/$ARCHIVE" \
            README.md LICENSE compose.infisical.yml \
            docs scripts web zapit deploy .env.example
          (cd dist && sha256sum "$ARCHIVE" > "${ARCHIVE}.sha256")
          ls -lh dist/
          echo "bundle=$ARCHIVE" >> "$GITHUB_OUTPUT"

      - name: Cut the GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.tag.outputs.version }}
        run: |
          set -euo pipefail
          VERSION="${VERSION}"
          NOTES=$(printf '%s\n' \
            "zapit **${VERSION}** — the TransferOps edge utility of the Innotel Platform Stack: ephemeral, peer-to-peer file transfer with no storage layer." \
            "" \
            "This release bundles the repository source + docs snapshot used to deploy the TransferOps layer." \
            "" \
            "**Artifact**" \
            "- zapit-${VERSION#v}.tar.gz + sha256" \
            "" \
            "**See also**" \
            "- [docs/stack.md](https://github.com/innotelinc/zapit/blob/main/docs/stack.md) — the owns/consumes map" \
            "- [docs/standard.md](https://github.com/innotelinc/innotel-platform-stack/blob/main/docs/standard.md) — the repo conformity standard" \
            "- [scripts/conform-project.sh](https://github.com/innotelinc/innotel-platform-stack/blob/main/scripts/conform-project.sh) — the scaffold/audit tool")
          ARCHIVE="zapit-${VERSION#v}.tar.gz"
          gh release create "$VERSION" \
            "dist/$ARCHIVE" \
            "dist/$ARCHIVE.sha256" \
            --title "zapit $VERSION" \
            --notes "$NOTES" || \
          gh release upload "$VERSION" \
            "dist/$ARCHIVE" \
            "dist/$ARCHIVE.sha256" --clobber
          echo "::notice::released $VERSION"