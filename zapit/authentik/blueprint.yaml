# Authentik blueprint for zapit — creates the OAuth2 provider + application in one shot.
#
# Apply it either way:
#   1) GitOps: mount into the authentik (worker) container, e.g. in authentik's compose:
#        - ./blueprint.yaml:/blueprints/zapit.yaml:ro
#      authentik discovers /blueprints/*.yaml automatically and applies it.
#   2) UI: Customize → Blueprints → Create, and paste this file.
#
# Environment variables honoured (set them on the authentik container):
#   ZAPIT_CLIENT_ID    OAuth2 client id zapit presents — must match AUTHENTIK_CLIENT_ID
#                      on zapit. Default: zapit
#
# Then configure zapit:
#   AUTHENTIK_BASE_URL=https://<your-authentik-host>
#   AUTHENTIK_CLIENT_ID=<same value as ZAPIT_CLIENT_ID>
#   PUBLIC_URL=https://zapp.innotel.us
#
# Finally flip "Require Authentik login" in zapit's /admin panel.
version: 1
metadata:
  name: zapit
entries:
  # OAuth2/OIDC provider — public client (PKCE, no secret needed by zapit)
  - identifiers:
      slug: zapit
    model: authentik_providers_oauth2.oauth2provider
    id: zapit-provider
    attrs:
      name: zapit
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: public
      client_id: !Env [ZAPIT_CLIENT_ID, zapit]
      redirect_uris:
        # canonical public host — matches zapit's PUBLIC_URL
        - matching_mode: strict
          url: https://zapp.innotel.us/api/auth/callback
          redirect_uri_type: authorization
        # local development
        - matching_mode: strict
          url: http://localhost:5150/api/auth/callback
          redirect_uri_type: authorization
        # optional LAN access — uncomment and adjust to your zapit host:
        # - matching_mode: strict
        #   url: http://192.168.1.10:5150/api/auth/callback
        #   redirect_uri_type: authorization
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
  # Application (what users click in authentik / my-applications)
  - identifiers:
      slug: zapit
    model: authentik_core.application
    id: zapit-application
    attrs:
      name: zapit
      slug: zapit
      provider: !KeyOf zapit-provider
