# zapit — platform environment (repo root).
# The app's own config lives in zapit/.env (see zapit/.env.example); this
# root .env carries the Innotel Platform Stack contract: secrets live in
# Infisical (SecretOps), and .env is derived from it.

# ─── Infisical (SecretOps) ─────────────────────────────────────────
# Generate the keys with:
#   openssl rand -base64 32   # INFISICAL_ENCRYPTION_KEY
#   openssl rand -hex 16      # INFISICAL_AUTH_SECRET
#   openssl rand -hex 16      # INFISICAL_DB_PASSWORD
# Then: docker compose -f zapit/docker-compose.yml -f compose.infisical.yml \
#         --profile infisical up -d
#       bash scripts/infisical-setup.sh
INFISICAL_PORT=8383
INFISICAL_ENCRYPTION_KEY=
INFISICAL_AUTH_SECRET=
INFISICAL_DB_PASSWORD=
INFISICAL_ADMIN_EMAIL=
INFISICAL_ADMIN_PASSWORD=
INFISICAL_ORG=Innotel
INFISICAL_WORKSPACE_NAME=zapit-secrets
# Runtime resolution contract (written back to .env by infisical-setup.py).
INFISICAL_ADDR=
INFISICAL_TOKEN=
INFISICAL_WORKSPACE_ID=
INFISICAL_ENVIRONMENT=prod